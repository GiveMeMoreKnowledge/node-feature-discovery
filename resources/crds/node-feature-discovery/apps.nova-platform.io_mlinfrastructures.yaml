---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: mlinfrastructures.apps.nova-platform.io
spec:
  group: apps.nova-platform.io
  names:
    kind: MLInfrastructure
    listKind: MLInfrastructureList
    plural: mlinfrastructures
    singular: mlinfrastructure
  scope: Cluster
  versions:
    - name: v1alpha1
      schema:
        openAPIV3Schema:
          description: MLInfrastructure is the Schema for the MLInfrastructure API
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: MLInfrastructureSpec defines the desired state of MLInfrastructure
              properties:
                nvidiaGpuOperator:
                  description: NvidiaGPUOperatorSpec configures Nvidia GPU Operator
                  properties:
                    mig:
                      description: MIGConfig defines Multi-Instance GPU configuration
                      properties:
                        configRef:
                          type: string
                        enabled:
                          type: boolean
                        strategy:
                          enum:
                            - none
                            - single
                            - mixed
                          type: string
                      required:
                        - enabled
                      type: object
                    timeSlicing:
                      description: TimeSlicingConfig defines GPU time-slicing parameters
                      properties:
                        configRef:
                          type: string
                        enabled:
                          type: boolean
                      required:
                        - enabled
                      type: object
                  required:
                    - mig
                    - timeSlicing
                  type: object
                nvidiaNetworkOperator:
                  description: NvidiaNetworkOperatorSpec configures Nvidia Network Operator
                  properties:
                    enabled:
                      type: boolean
                  type: object
              required:
                - nvidiaGpuOperator
              type: object
            status:
              description: MLInfrastructureStatus defines the observed state of MLInfrastructure
              properties:
                components:
                  description: Component statuses for Ready/Error phases
                  items:
                    description: ComponentStatus holds readiness status for a component
                    properties:
                      message:
                        description: Human-readable message
                        type: string
                      name:
                        description: Name of the component
                        type: string
                      reason:
                        description: Reason for the status
                        enum:
                          - AllKustomizationsReady
                          - ApplyFailed
                          - WaitingForKustomization
                          - KustomizationNotFound
                        type: string
                      status:
                        description: "Status of the component: true if ready"
                        type: boolean
                      type:
                        description: Type is always "ready"
                        enum:
                          - ready
                        type: string
                    required:
                      - name
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                conditions:
                  description: Conditions describe current processing stages
                  items:
                    description: MLInfrastructureCondition represents a readiness condition
                    properties:
                      lastTransitionTime:
                        description: LastTransitionTime is the time of last status change
                        format: date-time
                        type: string
                      message:
                        description: Human-readable message
                        type: string
                      reason:
                        description: Reason for the condition
                        enum:
                          - ReconciliationTriggered
                          - WaitingForKustomization
                          - AllKustomizationsReady
                          - ApplyFailed
                        type: string
                      status:
                        description: "Status of the condition: true if complete"
                        type: boolean
                      type:
                        description: Type is always "ready"
                        enum:
                          - ready
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                observedGeneration:
                  description: ObservedGeneration is the last reconciled generation
                  format: int64
                  type: integer
                phase:
                  description: Current phase of the resource lifecycle
                  enum:
                    - Pending
                    - Reconciling
                    - Ready
                    - Error
                  type: string
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
